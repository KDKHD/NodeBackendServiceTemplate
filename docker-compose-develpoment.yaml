version: "3.8"
services:

  database:
    hostname: database
    container_name: database
    image: postgres:14.1
    environment:
      POSTGRES_DB: "postgres"
      POSTGRES_PASSWORD: "changeme"
      POSTGRES_USER: "postgres"
      PGPORT: 5432
    ports:
      - "5432:5432"
    networks:
      - service

networks:
  service: