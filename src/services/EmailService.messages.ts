import { Messages } from "@type/message";

const emailServiceMessages: Messages = {
  existingEmail: {
    id: 'email.service.email.already.exists',
    defaultMessage: '{} already exists. Please use a different email.',
  },
};

export { emailServiceMessages };
